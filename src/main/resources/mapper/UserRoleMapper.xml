<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bryan.system.mapper.UserRoleMapper">

    <resultMap id="BaseResultMap" type="com.bryan.system.domain.entity.SysUserRole">
        <id column="id" property="id"/>
        <result column="role_name" property="roleName"/>
        <result column="is_default" property="isDefault"/>
    </resultMap>

    <select id="selectOneByIsDefaultTrue" resultMap="BaseResultMap">
        SELECT id, role_name, is_default
        FROM sys_user_role
        WHERE is_default = TRUE
        LIMIT 1
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT id, role_name, is_default
        FROM sys_user_role
    </select>

    <select id="selectByIdList" resultMap="BaseResultMap">
        SELECT id, role_name, is_default
        FROM user_role
        WHERE id IN
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </select>
</mapper>